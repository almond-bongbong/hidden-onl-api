input FileInput {
  url: String!
  originalName: String!
  size: Int
}

input LocationInput {
  latitude: Float!
  longitude: Float!
  zipcode: Int
  address: String
  addressDetail: String
}

input LatLngInput {
  latitude: Float!
  longitude: Float!
}